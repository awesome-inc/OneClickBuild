<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="XUnitSample.csproj"/>
  <Target Name="foo" BeforeTargets="PrepareForBuild" Condition="'$(IsCSharpProject)' != 'true'">
     <Message Importance="High" Text="Not a C# project: file: '$(MSBuildProjectFile)' ..." />
     <Message Importance="High" Text="UpdateAssemblyInfo ? '$(UpdateAssemblyInfo)' ..." />
  </Target>
</Project>